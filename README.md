# Dream-Job-Finder
A terminal-based job search tool powered by the JSearch API. Allows users to filter jobs by role, location, remote preference, salary, and job type, and saves results as CSV files.
